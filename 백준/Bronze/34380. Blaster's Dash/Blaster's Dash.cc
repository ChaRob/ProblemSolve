#include <iostream>
using namespace std;

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    int m, v, n, k;
    cin >> m >> v >> n >> k;
    cout << 2 * n + 40;
    return 0;
}