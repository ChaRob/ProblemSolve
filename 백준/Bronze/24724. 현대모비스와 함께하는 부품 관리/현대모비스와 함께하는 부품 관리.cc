#include <iostream>
#include <vector>
using namespace std;

int main()
{
	cin.tie(NULL);
	ios::sync_with_stdio(false);
	int T;
	cin >> T;
	for (int t = 1; t <= T; t++)
	{
		cout << "Material Management " << t << "\n";
		int n, a, b;
		cin >> n >> a >> b;
		for (int i = 0; i < n; i++)
		{
			int u, v;
			cin >> u >> v;
		}
		cout << "Classification ---- End!\n";
	}
	return 0;
}